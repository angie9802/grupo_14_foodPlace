<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head.ejs')  %> 
    <title>Users</title>
    <link rel="stylesheet" href="/css/normalize.css" />
    <link rel="stylesheet" href="/css/manage.css" />
  </head>
  <body>
 
       <%- include('./partials/header')%>

      <main>
          <h1>Manage Users</h1>
          <section class="container-ext">
            <section class="container-int">
              <table class="tftable" >
                <tr><th class="left"><p>ID</p></th><th><p>Name</p></th><th> <p>See</p></th><th><p>Edit</p></th><th  class="right"><p>Delete</p></th></tr>
                <% users.forEach(user => { %>
                    <tr>
                      <td class="left"><%= user.id%> </td>
                      <td><%= user.fullname.replace(/\b\w/g, l => l.toUpperCase())%> </td>
                      <td><a href="/users/<%= user.id%> "> <i class="fas fa-eye"></i></a></td>
                      <td><a href="/users/admin/edit/<%= user.id%> "> <i class="fas fa-edit"></i></a></td>
                      <td class="right">
                        <form action="/users/delete/admin/<%= user.id%>?_method=DELETE" method="POST">
                          <button class="delete">
                            <i class="far fa-trash-alt"></i>
                          </button>
                        </form>
                      </td>
                    </tr>
                    <% }); %>
                </table>
            </section> 
        </section>
    </main>
  
    <footer class="f-cart">
      <%- include('./partials/footer.ejs') %>
      </footer>
  </body>
</html>
