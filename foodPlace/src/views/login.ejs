<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head.ejs')  %> 
    <title>Login</title>
    <link rel="stylesheet" href="/css/login.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins&family=Roboto&display=swap" rel="stylesheet">  
    
</head>
<body>
    <main>
        <form action="/users/login" class="iniciosesion" method="POST">
        <div class="login">
            <h1 class="title">Login</h1>
            <h3 class="subtitle">Sign in to your account</h3>
            <div class="username">
                <input type="text" name="email" id="email" placeholder="Email">
                <% if (locals.errors && errors.email) { %>
                  <div class="text-danger">
                    <%= errors.email.msg %>
                  </div>
                <%} %>
                <div class="text-danger no-visible" id="email-empty">
                    Enter your email
                </div>
                <div class="text-danger no-visible" id="email-invalid">
                    Invalid Email
                </div>
            </div>
            <div class="password">
                <input type="password" name="password" id="password" placeholder="Password">
                <% if (locals.errors && errors.password) { %>
                    <div class="text-danger">
                      <%= errors.password.msg %>
                    </div>
                  <%} %>
                  <div class="text-danger no-visible" id="password-empty">
                    Enter your password
                </div>
                <p><label for="password"> Do you forget your password? <a href="register">Click here</a></label></p>
            </div>
            <div class="remember">
                <label for="rememberme">Remember me </label>
                <input type="checkbox" name="remember" id ="rememberme" value="rememberme">
            </div>
            <div class="botonlogin">
                <a href="/"><button type="submit">Login</button></a>
                <p>if you don't have an account, <a href="register">Register now</a></p>
            </div>
            <div class="backhome">
                <a href="/"><i class="fas fa-caret-left"></i></a>
            </div>
        </div>
    </form>
    </main>
</body>
<script src="/js/login.js"></script>
</html>